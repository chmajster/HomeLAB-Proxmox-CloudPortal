(() => {
  'use strict';
  if (document.body.dataset.page !== 'admin-resource-details') return;

  const body=document.body,content=document.getElementById('appContent'),pageTitle=document.getElementById('pageTitle');
  const basePath=body.dataset.basePath||'',csrf=body.dataset.csrf||'',locale=document.documentElement.lang==='en'?'en':'pl';
  const appUrl=path=>`${basePath}${path}`;
  const h=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
  const labels=locale==='pl'
    ?{users:'Użytkownik',proxmox:'Połączenie Proxmox',networks:'Sieć',templates:'Template VM',storages:'Storage',plans:'Plan zasobów'}
    :{users:'User',proxmox:'Proxmox connection',networks:'Network',templates:'VM template',storages:'Storage',plans:'Resource plan'};
  const lists=locale==='pl'
    ?{users:'Użytkownicy',proxmox:'Połączenia Proxmox',networks:'Sieci',templates:'Template',storages:'Storage',plans:'Plany zasobów'}
    :{users:'Users',proxmox:'Proxmox connections',networks:'Networks',templates:'Templates',storages:'Storage',plans:'Resource plans'};
  const fields={id:'ID',name:locale==='pl'?'Nazwa':'Name',username:'Login',email:'E-mail',status:'Status',role:locale==='pl'?'Rola':'Role',locale:locale==='pl'?'Język':'Language',session_version:locale==='pl'?'Wersja sesji':'Session version',last_login_at:locale==='pl'?'Ostatnie logowanie':'Last login',created_at:locale==='pl'?'Utworzono':'Created',updated_at:locale==='pl'?'Zaktualizowano':'Updated',hostname:'Host',port:'Port',realm:'Realm',api_token_id:'API token',verify_ssl:'TLS verification',cluster_name:locale==='pl'?'Klaster':'Cluster',last_checked_at:locale==='pl'?'Ostatni test':'Last check',last_error:locale==='pl'?'Ostatni błąd':'Last error',connection_id:'Connection ID',connection_name:locale==='pl'?'Połączenie':'Connection',node_name:locale==='pl'?'Węzeł':'Node',bridge:'Bridge',vlan_id:'VLAN',subnet:'Subnet',gateway:locale==='pl'?'Brama':'Gateway',dns_servers:'DNS',enabled:locale==='pl'?'Aktywny':'Enabled',address_count:'IP addresses',free_count:'Free IP',reserved_count:'Reserved IP',allocated_count:'Allocated IP',vmid:'VMID',operating_system:'OS',description:locale==='pl'?'Opis':'Description',storage_name:'Storage',content_types:'Content',slug:'Slug',vcpu:'vCPU',ram_mb:'RAM (MB)',disk_gb:'Disk (GB)',allow_resize:'Resize',sort_order:'Sort order'};
  const relatedLabels=locale==='pl'?{projects:'Projekty',vms:'Maszyny wirtualne',nodes:'Węzły',networks:'Sieci',storages:'Storage',templates:'Template'}:{projects:'Projects',vms:'Virtual machines',nodes:'Nodes',networks:'Networks',storages:'Storage',templates:'Templates'};

  async function api(path,options={}){
    const init={...options,headers:{Accept:'application/json',...(['GET','HEAD'].includes(options.method||'GET')?{}:{'X-CSRF-Token':csrf}),...(options.body?{'Content-Type':'application/json'}:{}),...(options.headers||{})}};
    if(options.body&&typeof options.body!=='string')init.body=JSON.stringify(options.body);
    const response=await fetch(appUrl(path),init);let payload;try{payload=await response.json();}catch{throw new Error(`HTTP ${response.status}`);}
    if(!response.ok)throw new Error(payload.error?.message||`HTTP ${response.status}`);return payload.data;
  }
  function current(){const path=location.pathname.startsWith(basePath)?location.pathname.slice(basePath.length):location.pathname;const match=path.match(/^\/admin\/(users|proxmox|networks|templates|storages|plans)\/(\d+)$/);return match?{resource:match[1],id:Number(match[2])}:null;}
  function display(raw,key){if(raw===null||raw===undefined||raw==='')return'—';if(['enabled','allow_resize','verify_ssl'].includes(key))return Number(raw)===1?(locale==='pl'?'Tak':'Yes'):(locale==='pl'?'Nie':'No');return String(raw);}
  function summary(record){const hidden=new Set(['password_hash','api_token_secret_encrypted']);return Object.entries(record).filter(([key])=>!hidden.has(key)).map(([key,raw])=>`<div class="summary-item"><small>${h(fields[key]||key.replaceAll('_',' '))}</small><span class="text-break">${h(display(raw,key))}</span></div>`).join('');}
  function related(name,rows){if(!Array.isArray(rows)||!rows.length)return'';const keys=Object.keys(rows[0]).slice(0,8);return `<section class="panel mt-3"><div class="panel-header"><h2 class="h5 mb-0">${h(relatedLabels[name]||name)}</h2><span class="resource-meta">${rows.length}</span></div><div class="table-responsive"><table class="table"><thead><tr>${keys.map(key=>`<th>${h(fields[key]||key.replaceAll('_',' '))}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${keys.map(key=>`<td class="text-break">${h(display(row[key],key))}</td>`).join('')}</tr>`).join('')}</tbody></table></div></section>`;}
  function actions(resource,record){
    if(resource==='users')return `<section class="panel mt-3" id="account"><div class="panel-header"><h2 class="h5 mb-0">${locale==='pl'?'Zarządzanie kontem':'Account management'}</h2></div><div class="panel-body"><form id="userUpdate" class="row g-3"><div class="col-md-4"><label class="form-label">Status</label><select class="form-select" name="status"><option value="active" ${record.status==='active'?'selected':''}>active</option><option value="blocked" ${record.status==='blocked'?'selected':''}>blocked</option><option value="pending" ${record.status==='pending'?'selected':''}>pending</option></select></div><div class="col-md-4"><label class="form-label">${locale==='pl'?'Rola':'Role'}</label><select class="form-select" name="role"><option value="user" ${record.role==='user'?'selected':''}>user</option><option value="admin" ${record.role==='admin'?'selected':''}>admin</option></select></div><div class="col-md-4" id="password"><label class="form-label">${locale==='pl'?'Nowe hasło (opcjonalnie)':'New password (optional)'}</label><input class="form-control" name="password" type="password" minlength="12" autocomplete="new-password"></div><div class="col-12"><button class="btn btn-primary" type="submit">${locale==='pl'?'Zapisz zmiany':'Save changes'}</button></div></form></div></section>`;
    if(resource==='proxmox')return `<section class="panel mt-3" id="connection"><div class="panel-header"><h2 class="h5 mb-0">${locale==='pl'?'Zarządzanie połączeniem':'Connection management'}</h2></div><div class="panel-body"><form id="proxmoxUpdate" class="row g-3"><div class="col-md-4"><label class="form-label">Status</label><select class="form-select" name="status"><option value="active" ${record.status==='active'?'selected':''}>active</option><option value="disabled" ${record.status==='disabled'?'selected':''}>disabled</option></select></div><div class="col-md-8" id="secret"><label class="form-label">${locale==='pl'?'Nowy sekret tokenu API (opcjonalnie)':'New API token secret (optional)'}</label><input class="form-control" name="api_token_secret" type="password" autocomplete="new-password"></div><div class="col-12 d-flex gap-2"><button class="btn btn-primary" type="submit">${locale==='pl'?'Zapisz zmiany':'Save changes'}</button><button class="btn btn-outline-primary" id="syncProxmox" type="button">Test / Sync</button></div></form></div></section>`;
    if(['networks','templates','storages','plans'].includes(resource)){const enabled=Number(record.enabled)===1;return `<section class="panel mt-3"><div class="panel-header"><h2 class="h5 mb-0">${locale==='pl'?'Stan zasobu':'Resource state'}</h2></div><div class="panel-body"><button class="btn ${enabled?'btn-outline-warning':'btn-outline-success'}" id="toggleResource" type="button">${enabled?(locale==='pl'?'Wyłącz':'Disable'):(locale==='pl'?'Włącz':'Enable')}</button></div></section>`;}return'';
  }
  function bind(resource,id,record){
    document.getElementById('userUpdate')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,values=Object.fromEntries(new FormData(form));if(!values.password)delete values.password;await api(`/api/v1/admin/users/${id}`,{method:'PATCH',body:values});await render(resource,id);});
    document.getElementById('proxmoxUpdate')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget,values=Object.fromEntries(new FormData(form));if(!values.api_token_secret)delete values.api_token_secret;await api(`/api/v1/admin/proxmox/${id}`,{method:'PATCH',body:values});await render(resource,id);});
    document.getElementById('syncProxmox')?.addEventListener('click',async event=>{const button=event.currentTarget;button.disabled=true;try{await api(`/api/v1/admin/proxmox/${id}/sync`,{method:'POST'});await render(resource,id);}finally{if(document.body.contains(button))button.disabled=false;}});
    document.getElementById('toggleResource')?.addEventListener('click',async event=>{const button=event.currentTarget;button.disabled=true;try{await api(`/api/v1/admin/${resource}/${id}`,{method:'PATCH',body:{enabled:Number(record.enabled)!==1}});await render(resource,id);}finally{if(document.body.contains(button))button.disabled=false;}});
  }
  async function render(resource,id){const data=await api(`/api/v1/admin/details/${resource}/${id}`),record=data.record||{},title=record.name||record.username||record.storage_name||`${labels[resource]} #${id}`;pageTitle.textContent=title;content.innerHTML=`<a class="btn btn-outline-secondary mb-3" href="${appUrl('/'+resource)}">← ${h(locale==='pl'?'Wróć: ':'Back to ')}${h(lists[resource])}</a><section class="panel"><div class="panel-header"><div><p class="eyebrow mb-0">${h(labels[resource])}</p><h2 class="h4 mb-0">${h(title)}</h2></div><span class="status-badge status-${h(record.status||(Number(record.enabled)===1?'active':'disabled'))}">${h(record.status||(Number(record.enabled)===1?'active':'disabled'))}</span></div><div class="panel-body"><div class="summary-list">${summary(record)}</div></div></section>${actions(resource,record)}${Object.entries(data.related||{}).map(([name,rows])=>related(name,rows)).join('')}`;bind(resource,id,record);if(location.hash&&['#password','#secret','#account','#connection'].includes(location.hash))document.querySelector(location.hash)?.scrollIntoView({block:'start'});}
  (async()=>{const item=current();try{if(!item)throw new Error(locale==='pl'?'Nieprawidłowy adres zasobu.':'Invalid resource URL.');await render(item.resource,item.id);}catch(error){pageTitle.textContent=locale==='pl'?'Szczegóły zasobu':'Resource details';content.innerHTML=`<div class="alert alert-danger"><h2 class="h5">${locale==='pl'?'Nie udało się pobrać danych.':'Unable to load resource.'}</h2><p class="mb-0">${h(error.message||error)}</p></div>`;}})();
})();
