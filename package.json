{
  "name": "algen-proxmox-cloudportal-tests",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:proxmox-e2e": "playwright test --config=playwright.proxmox.config.js",
    "test:visual": "node tests/visual-check.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
